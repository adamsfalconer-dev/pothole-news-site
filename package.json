{
  "name": "pothole-news-site",
  "private": true,
  "description": "Pothole News — the public static site (built by build-site.js, deployed on Cloudflare Pages).",
  "scripts": {
    "verify": "node tools/verify-clean.js .",
    "build": "node tools/verify-clean.js . && node tools/build-site.js",
    "serve": "node tools/serve-dist.js"
  },
  "engines": {
    "node": ">=20"
  }
}
